import {Divider} from "rsuite";

import styles from './home.module.scss'


export const Home = () => {
  return (
    <div className={styles.wrapper}>
      <Divider className={styles.divider}>Страница в разработке</Divider>
    </div>
  );
};
